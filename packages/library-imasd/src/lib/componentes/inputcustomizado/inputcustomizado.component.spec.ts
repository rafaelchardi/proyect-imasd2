import { ComponentFixture, TestBed } from '@angular/core/testing';

describe('InputcustomizadoComponent', () => {
/*   let component: InputcustomizadoComponent;
  let fixture: ComponentFixture<InputcustomizadoComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ InputcustomizadoComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(InputcustomizadoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
